enum SystemMessages {
    Connected = "Connected to the network",
    Disconnected = "Disconnected from the network",
    HighLatency = "Your connection may be slow due to high latency"
}

export default SystemMessages;
