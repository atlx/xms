import React, {Component} from "react";
import "../../styles/contacts/contactsBar.scss";

export default class ContactsBar extends Component {
    public render(): JSX.Element {
        return (
            <div className="contacts-bar">

            </div>
        );
    }
}
